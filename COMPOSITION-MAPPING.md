# 体脂率与肌肉量：近似外形映射与分区响应 v4

本模块采用近似外形模拟。界面输入体脂率（%）和肌肉量（骨骼肌 kg），模型仅作示意。输入数值不是从网格测得，也不是人体测量、体成分诊断或训练结果预测。不把去脂体重等同骨骼肌质量，也不从这两个输入推算真实体重。

## 原始资料与边界

当前源模型的 Muscle/Weight 是形态混合控制；[MakeHuman 官方说明](https://static.makehumancommunity.org/makehuman/docs/modeling_the_body.html)介绍了肌肉与体重外形控制，[官方 human.py](https://github.com/makehumancommunity/makehuman/blob/master/makehuman/apps/human.py)将肌肉控制描述为 0–1 的外形范围。此次核查没有取得可直接用于当前网格的体脂百分比/骨骼肌公斤数标定。

下列数值全部是本产品的公开示意假设，不是来自这些官方资料的医学换算系数，也不是男女平均值或健康范围。

## 映射规则

旧版可用形态刻度为 15 / 50 / 85。新输入用三个锚点分段线性映射到这些刻度：

| 模型 | 参考身高 | 体脂率三个锚点 | 骨骼肌量三个锚点 |
|---|---|---|---|
| 男性 | 175 cm | 5 / 20 / 50 % | 18 / 32 / 46 kg |
| 女性 | 165 cm | 5 / 28 / 50 % | 12 / 22 / 32 kg |

骨骼肌锚点随身高按 `当前身高³ / 参考身高³` 缩放，表示相似形状体积缩放的建模约定，不是生理学公式。固定 kg 时，身高越高，相对肌肉外形越小。

输入范围：身高 145–200 cm；体脂率 5–50%；骨骼肌量 5–80 kg。它们只是交互范围，不是健康建议。超出当前身高对应的肌肉外形锚点范围时保留输入数值，形态显示最近端点，并在当前编辑对象下明确提示范围和饱和状态。

差值显示：体脂率用“百分点”（例如 20% → 25% 为 +5.0 个百分点）；肌肉量用 kg。不存在将体脂率差当作体脂质量差的计算。

## 分区外形响应（2026-09-18）

`human.js` 将平均体型作为基线，分别叠加平均脂肪下的肌肉目标、平均肌肉下的脂肪目标；`regional-shape.js` 在中性人体坐标下定义连续的区域响应，不用一个全身缩放系数替代二者。

- 肌肉：肩背轮廓加宽；上臂、大腿、小腿围绕各自骨段轴增加横截面，避免用两腿外移冒充大腿增粗；腰背比例和腹部平整度作小幅示意调整。
- 脂肪：腹部前向、腰侧横向、臀部后向及四肢软组织分别增加。v3增强上臂/前臂/大腿/小腿横截面，并向关节平滑衰减，避免只有肌腹鼓起。脸颊、下颌、颏下及颈部采用相对头部长度的连续场；保留颅顶与眼鼻主结构，避免把整颗头放大。低体脂端用较弱的收缩幅度。幅度是造型约定，不是医学系数。
- 姿态间隙：较大的体型会小幅增加上臂外展，避免自然下垂的手陷入髋部/裙侧；保留所选姿态，A/B 的关节角可能因此略有不同。
- 两项输入独立，外形联动：v4在独立基础形态上叠加肌肉支撑/脂肪覆盖。高脂低肌追加腹部前突和小幅下移；高肌体型增加肩胸背与四肢体积，脂肪越高，局部肌肉轮廓越柔和。基础脂肪层始终保留，不因增肌自动消失。默认人体中间值不变。
- 骨骼宽度、肌肉分布、脂肪分布存在个人差异；这里的区域与幅度是明确的造型规则，不能从两个输入唯一确定一个真实人体。没有声称质量守恒、组织体积测量或训练结果预测。

`validate-regional.mjs` 在同一组解剖顶点上检查背宽、单侧大腿宽、腰背比、腹部前突及高肌肉下的脂肪保留；测试的是产品外形规则，不是临床有效性。

## 默认值与旧文件

默认男模 175 cm / 20% / 32 kg；女模 165 cm / 28% / 22 kg，均为可调整示意预设。默认外形与旧版中间刻度一致。

JSON 升为 version 2，A/B 保存 `height`、`bodyFatPercent`、`skeletalMuscleKg`，附带 `compositionModel: soma-regional-v4`。version 1 的 `fat/muscle` 按逆映射转换为新单位，内部保留数值精度，但外形按本版分区规则重新生成；显示值保留一位小数。不会把原来的 50 直接解释为 50% 或 50 kg。

页面自动化工具新增 `bodyFatPercent` / `skeletalMuscleKg`。原 `fat` / `muscle` 参数保留旧刻度语义用于兼容，不允许和新单位混用。无效输入不更新人物状态或历史缓存。

验证入口：`node validate-composition.mjs` 检查旧/新数据往返、同版换算几何一致、增减方向、身高影响、饱和提示和新旧工具兼容；这些测试不能建立医学有效性。

此前 `soma-illustrative-v1`、`soma-regional-v2` 和 `soma-regional-v3` 文件仍可读，数值保留，并提示使用新版分区规则；非默认参数的外形有意改变，不能声称与旧版网格逐点一致。旧版外形不保证与本版逐点相同。

新增验证：`validate-fat-distribution.mjs` 固定身高与肌肉量，以同一解剖顶点测量两侧四肢到中性骨轴的平均半径、脸颊与下颌半宽，检查逐步增脂响应及颅顶稳定。该检查对修改前版本失败（男性前臂仅约8.9%增幅），本版通过；这只是对建模验收阈值的检验，并非人体脂肪分布校准。

## v4 四种体态示例与验收

示例始终保留当前编辑对象的身高。男性低/高体脂为10%/40%，女性18%/45%；低/高肌肉采用该性别当前身高下的两端示意锚点，四舍五入到0.1 kg。这些值只用于直观看到四种形态，不是体型诊断阈值、平均值或健康建议。

肌肉的区域峰值与脂肪的连续软组织覆盖分开处理；低肌高脂再添加腹部支撑不足的造型差异。躯干字段按髋关节与肩部的相对位置计算，避免男性坐标用于女性。肩背变宽和胸/四肢饱满共同构成高肌形态，腰部保持合理宽度；女性胸部增肌的附加场侧重上胸而非乳房前端。

`validate-body-quadrants.mjs` 使用同一组皮肤顶点测量肩背宽、腰背比例、腹部前向和垂直位置、肢体径向厚度，检查四种组合的区别。特别检查高脂高肌仍比低脂高肌腹部更厚，同时高脂低肌腹部更低、更前突。v2/v3的严格位移线性独立约束已被这组交互要求取代，并非取消体脂与肌肉两个输入的独立性。验证通过不等于生物力学或生理标定。
